#!/usr/bin/env ruby

lib = File.expand_path('../../lib', __FILE__)
$:.unshift(lib) unless $:.include?(lib)

require 'config/boot'
require 'corporeal/config'
require 'corporeal/database'
require 'corporeal/handler'

Corporeal::Database.prepare!
Corporeal::Handler.run!
